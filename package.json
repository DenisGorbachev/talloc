{
  "name": "talloc",
  "version": "1.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "watch": "coffee --map --compile --transpile --output dist/ --watch src/",
    "build": "coffee --map --compile --transpile --output dist/ src/",
    "prepare": "npm run build"
  },
  "main": "dist/index.js",
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.4.5",
    "@shelf/jest-mongodb": "^1.0.1",
    "babel-jest": "^24.8.0",
    "jest": "^24.8.0",
    "jest-preset-coffeescript": "^1.0.1"
  },
  "dependencies": {
    "@hapi/joi": "^15.1.0",
    "coffeescript": "^2.4.1",
    "lodash": "^4.17.11",
    "moment": "^2.24.0",
    "mongodb": "^3.2.7"
  }
}
